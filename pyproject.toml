[project]
name = "codex-effort-router"
version = "0.0.1"
description = "Skill-driven Codex model and reasoning-effort router"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
